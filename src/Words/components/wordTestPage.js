import React from 'react';

// components
import { WrappedHorizontalLoginForm } from './WordTest';

const WordTest = () => (
  <div className="wordsTest">
    <WrappedHorizontalLoginForm />
  </div>
);
export default WordTest;
